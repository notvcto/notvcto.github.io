import { AppID } from '@/store/system';

export const firefoxManifest = {
  id: 'firefox' as AppID,
  name: 'Firefox',
  icon: 'public',
  defaultSize: { width: 800, height: 600 },
  singleton: true,
};
