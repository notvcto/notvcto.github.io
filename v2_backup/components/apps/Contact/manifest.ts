import { AppID } from '@/store/system';

export const contactManifest = {
  id: 'contact' as AppID,
  name: 'Contact',
  icon: 'email',
  defaultSize: { width: 400, height: 500 },
  singleton: true,
};
